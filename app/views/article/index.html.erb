<h1>Hello,Rails Server</h1>
<table>
    <tr>
        <th>Id</th>
        <th>Title</th>
        <th>Body</th>
    </tr>

    <% @articles.each do |article| %>
    <tr>

    		<td><%= article.id %></td>
        <td><%= article.title %></td>
        <td><%= article.body %></td>
    </tr>
    <% end %>
</table> 